[project]
name = "shared"
version = "0.1.0"
description = "Shared constants and utilities for sim2real"
requires-python = ">=3.11.2"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[tool.uv-build-backend]
module-root = "src"
